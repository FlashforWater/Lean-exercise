# Mistakes To Collect

Use this file as a learning log. Good entries are small and concrete.

## Template

- Date:
- What I tried:
- Error message:
- What the error meant:
- Fix:

## First Notes

- `sorry` means "accept this proof for now." It is useful while learning, but completed files should eventually remove it.
- `rfl` proves goals where both sides reduce to the same expression.
- `exact h` works when `h` has exactly the type of the current goal.
